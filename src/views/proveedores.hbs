<div class="container p-5" style="background-color: #2762c9; color: #f0e3e3;">
  <div class="row">
    <div class="col-md-5">
      <h1>Agregar Proveedor</h1>
      <form action="/proveedores/add" method="POST">
        <div class="mb-3">
          <label for="nombre">Nombre</label>
          <input type="text" name="nombre" placeholder="Ingresa un nombre" class="form-control" required>
        </div>
        <div class="mb-3">
          <label for="paterno">Descripción</label>
          <input type="text" name="description" placeholder="Ingresa descripción" class="form-control" required>
        </div>
        <div class="d-flex justify-content-between mt-2">
          <button class="btn btn-success">Guardar</button>
          <a href="/menu" class="btn btn-warning">Regresar</a>
        </div>
      </form>
    </div>
    
    <div class="col-md-7">
      <h1>Lista de proveedores</h1>
      <table class="table">
        <thead>
          <tr>
            <th>Nombre</th>
            <th>Descripción</th>
            <th>Acciones</th>
          </tr>
        </thead>
        <tbody>
          {{#each proveedores}}
            <tr>
              <td>{{this.nombre}}</td>
              <td>{{this.description}}</td>
              <td>
                <a href="/proveedores/edit/{{this._id}}" class="btn btn-warning btn-sm">Editar</a>
                <a href="/proveedores/delete/{{this._id}}" class="btn btn-danger btn-sm">Eliminar</a>
              </td>
            </tr>
          {{/each}}
        </tbody>
      </table>
      <img src="img/frituta.png" alt="Imagen en esquina" class="corner-image" 
      style="display: block; margin-top: 50; width: 50; height: auto;">
    </div>
  </div>
</div>
